# Project Instructions — ΣLang

ΣLang 是一个 AI 原生语义协议（AI-Native Semantic Protocol）项目：规范 + 三个独立验证器 +
共享语料 + 证明工具。完整自主维护指令见 `AUTOPILOT.md`；**每次会话先读 AUTOPILOT.md 再动手**。

## 核心约定

- **规范优先**：规范（spec/）→ 检查（三验证器）→ 语料（corpus/）三者一体，改动须同步。
- **三验证器一致（Law XIII 门禁）**：任何改动后必须 `python3 verify_consensus.py` 全绿
  （当前 30/30，Python/Rust/Elixir 判定一致），并保持三端 0 warning。
- **禁止掩盖失败**：不得删除/注释/弱化测试或检查来让失败消失——修复根因。
- **证明工具**：`tools/sigma-prove.py`（z3 义务消解）、`tools/sigma-moonbit.py`（MoonBit 翻译桥）。
- **文档与实现同步**：README / MASTER_PLAN / spec 中的模块数（N/N）与状态须与实现一致。

## 常用命令

```sh
python3 verify_consensus.py                        # 三方共识（Law XIII 门禁）
python3 verify_p0.py                               # 算法检查 95/95
python3 tools/sigma-prove.py corpus/proof_max.md   # 证明消解（需 z3）
cd impl/verifier && cargo build                    # Rust：0 error / 0 warning
cd impl/elixir_rt && elixir sigma_verify.exs ../../corpus/arith_ok.md  # Elixir 单测
```

## 禁区

- 不要修改 `archive/`（历史归档）。
- 不要改动 `spec/zh/` 而不改英文原版（英文为准，中文为参考）。
- 常量（§C：`0xK0xx` 数学 / `0xQ0xx` 物理）与数学域符号（⊕ ⊗ ⊖ ⊘ ⊙ ≡ ≥ ≤ ∈ ℕ ℤ ℚ ℝ）不可遮蔽/重定义。

## 目标

推进到 **v0.10 可用**（所有数学符号、基本操作、常量包可用 + 证明可消解 + 共识门禁绿），
**只关心结果，不关心过程**。
